# AIOS Tests — Placeholder

Status: M0 — documentation only. No tests exist. No fake passing tests will be created.

## Future test areas

- state validation — illegal transitions rejected; class promotions audited.
- task lifecycle — status transitions and permission checks.
- permissions — role-scoped access (explorer read-only, etc.).
- context assembly — deterministic context from AIOS state.
- model routing — capability-based selection (M5).
- OpenClaw adapter — structured-result contract and validation (M6).

## Rules

- Tests are written only for implemented functionality.
- No fabricated tests, no self-passing stubs.

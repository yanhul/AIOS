# AIOS Model Registry — Placeholder

Status: M0 — documentation only. No models are registered, connected, or called.

## Intent

A future registry describing available models and their capabilities (capability-based routing). The registry will hold metadata only.

## Rules

- No API keys, no credentials, no secrets in this repository — ever.
- No model API is called from M0.
- Registry entries will be capability + configuration references, resolved at runtime by policy.
- Model routing belongs to M5 (see `docs/ROADMAP.md`).

## Current state

Empty. No entries.
